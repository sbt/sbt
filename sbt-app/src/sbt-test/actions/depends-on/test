-> a
-> b
-> c
-> d

$ touch succeed
> a
> b
> c
> d

> project input
-> j
-> f fail
> f succeed
-> g succeed

-> h fail
$ absent h
> set ThisBuild / traceLevel := 100
> set ThisBuild / logLevel := Level.Debug
> h succeed
$ exists h
