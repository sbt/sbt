# Test for #1870: forcing proxy repos via ~/.sbt/repositories_force + ~/.sbt/repositories.
# Scripted sets -Dsbt.global.base=<testDir>/global. We ship global/repositories_force and
# global/repositories so override is enabled before the child sbt starts; then overrideBuildResolvers is true.
> check
