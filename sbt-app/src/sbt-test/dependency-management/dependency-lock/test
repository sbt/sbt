# Test dependency lock file generation
> update
> dependencyLock

# Verify lock file was created
$ exists build.sbt.lock

# Check that lock file is valid
> dependencyLockCheck

# Test that useDependencyLock enables lock file usage
> set useDependencyLock := true
> update

# Modify dependencies and verify lock becomes stale
$ copy-file build.sbt build.sbt.backup
$ copy-file changes/build-modified.sbt build.sbt
> reload

# Lock check should now show stale warning (task still succeeds but returns false)
> dependencyLockCheck

# Update the lock file
> dependencyLockUpdate

# Now lock check should pass again
> dependencyLockCheck

# Restore original build
$ copy-file build.sbt.backup build.sbt
> reload
