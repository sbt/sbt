name := "drop-idle-servers-test"
scalaVersion := "3.7.4"

// Simple test that verifies the project compiles with the new sbt version
// Full drop-idle-servers feature testing requires manual testing with multiple sbt instances
