<ivysettings>
	<settings defaultResolver="databinder"/>
	<caches defaultCacheDir="${ivy.settings.dir}/target/cache"/>
	<resolvers>
		<ibiblio name="databinder" m2compatible="true" root="http://databinder.net/repo/"/>
	</resolvers>
</ivysettings> 